[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fish-bone"
version = "0.1"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]